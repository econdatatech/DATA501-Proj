% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/arPLS.R
\name{baseline_estimation}
\alias{baseline_estimation}
\title{asymmetrically reweighted penalized least squares}
\usage{
baseline_estimation(
  y,
  lambda = 1e+06,
  ratio = 1e-06,
  max_iter = 50,
  verbose = FALSE,
  algo = "banded"
)
}
\arguments{
\item{y}{Numeric vector representing the spectrum.}

\item{lambda}{Smoothing parameter. The smaller the more curvature (wiggliness). (default: 1e6).}

\item{ratio}{Stopping criterion based on changes in weight vector per iteration (default: 1e-6).}

\item{max_iter}{Maximum number of iterations as fall back criterion if no conversion happens (default: 50).}

\item{verbose}{Boolean to print intermediary outputs (default: FALSE).}

\item{algo}{String to choose solver between Armadillo CPP armaInv ("cpp") and native solver function "native" and limSolve::Solve.banded solver ("banded") (default: "banded").}
}
\value{
object of class arPLSresult:
\itemize{
  \item \code{rawinput}: The original spectrum fed into the algorithm.
  \item \code{lambda}: The lambda parameter fed into the algorithm.
  \item \code{ratio}: The ratio stopping parameter fed into the algorithm.
  \item \code{max_iter}: The maximum iteration stopping parameter fed into the algorithm.
  \item \code{baseline}: The fitted spectral baseline.
  \item \code{last_iter}: The number of iterations the algorithm did before stopping.
  \item \code{last_ratio}: The last value of the ratio stopping criterium before stopping.
}
}
\description{
Baseline estimation using asymmetrically reweighted penalized least squares smoothing (Baek et al. 2015).
}
\details{
The algorithm iteratively estimates a spectral baseline curve by updating a
weight vector by means of a generalized logistic function that focuses the
estimation efforts on regions where the baseline and the signal are close to
each other
}
\examples{
{
data("Abelsonite")
baseline <- baseline_estimation(Abelsonite$measurement, max_iter=10, verbose=T)
}
}
\references{
Baek, S.-J., Park, A., Ahn, Y.-J., and Choo, J. (2015). Baseline correction
using asymmetrically reweighted penalized least squares smoothing.
Analyst, 140:250–257.
}
\author{
Corvin Idler
}
